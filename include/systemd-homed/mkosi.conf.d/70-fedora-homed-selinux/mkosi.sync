#!/usr/bin/sh
set -euf

git_url=https://github.com/richiedaze/homed-selinux.git
repo=homed-selinux

workdir="${SRCDIR}/${repo}"

if ! [ -e "${workdir}/.git" ]; then
  git clone "${git_url}" "${workdir}"
else
  git -C "${workdir}" pull --ff-only --stat || true
fi
